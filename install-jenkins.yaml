- name: Install and Configure Jenkins
  hosts: all
  remote_user: ubuntu
  become: yes
  vars_files:
    - ./variables.yaml
    - ./secrets_file.yaml

  roles:
    - install-docker
    - install-git
    - install-jenkins
    - jenkinscasc
